"""ovf-mcp: a Model Context Protocol server for the Open Venture Framework (Equilibria).

The server lives in ``ovf_mcp.server``; run it with ``ovf-mcp`` or ``python -m ovf_mcp``.
It needs the optional ``mcp`` extra: ``pip install 'ovf[mcp]'``.
"""

__version__ = "0.1.0a0"
